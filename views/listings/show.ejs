<% layout("/layouts/boilerplate") %>
    <div class="row">
        <div class="col-8 offset-3 mt-3">
            <h3>
                <%= listing.title %>
            </h3>
        </div>
        <div class="col-6 offset-3">
            <div class="card listing-card">
                <img src="<%= listing.image.url %>" class="card-img-top show-img" alt=" <%= listing.title %> ">
                <div class="card-body">
                    <p class="card-text mt-3">
                    <p><i>@<%= listing.owner.username %></i></p>
                    <%= listing.description %> <br>
                        &#8377; <%= Number(listing.price).toLocaleString("en-IN") %> / night <br>
                            <%= listing.location %>,
                            <%= listing.country %>
                            <br>
                    <div>
                        <% for(let category of listing.category) { %>
                            <a href="/listings?category=<%= category %>" class="badge rounded-pill text-bg-secondary p-2 mt-3" style="text-decoration: none;"><%= category.toLocaleString().charAt(0) + category.toLocaleString().substring(1).toLowerCase() %></a>
                        <% } %>
                    </div>
                    </p>
                </div>
                <% if(currUser && currUser._id.equals(listing.owner._id)) { %>
                    <div class="btns mt-3">
                        <form action="/listings/<%= listing._id %>/edit" method="get">
                            <button type="submit" class="btn btn-outline-danger">Edit</button>
                        </form>
                        <form action="/listings/<%= listing._id %>?_method=DELETE" method="post">
                            <button type="submit" class="btn btn-dark offset-2">Delete</button>
                        </form>
                    </div>
                    <% } %>
            </div>
        </div>
        <div class="col-6 offset-3">
            <% if(listing.reviews.length !=0 && listing.reviews.length){ %>
                <hr>
                <div>
                    <h4>Reviews (<%= listing.reviews.length %>) </h4>
                    <div class="row mt-3">
                        <% for(let review of listing.reviews){ %>
                            <div class="card col-5 me-3 mb-3 p-3" style="width: 18rem;">
                                <div class="card-body">
                                    <h5 class="card-title">
                                        <%= review.author.username %>
                                    </h5>
                                    <p class="starability-result" data-rating="<%= review.rating %>"></p>
                                    <p class="card-text">
                                        <%= review.comment %>
                                    </p>
                                    <% if(currUser && currUser._id.equals(review.author._id)) { %>
                                        <form
                                            action="/listings/<%= listing._id %>/review/<%= review._id %>?_method=DELETE"
                                            method="POST">
                                            <button type="submit" class="btn btn-outline-dark">Delete</button>
                                        </form>
                                        <% } %>
                                </div>
                            </div>
                            <% } %>
                    </div>
                </div>
                <% } %>
                    <% if(currUser) { %>
                        <form action="/listings/<%= listing._id %>/review" method="POST"
                            class="form needs-validation mb-3" novalidate>
                            <hr>
                            <h3>Leave a review</h3>
                            <label class="form-label">Rating</label>
                            <fieldset class="starability-coinFlip">
                                <input type="radio" id="no-rate" class="input-no-rate" name="review[rating]" value="1"
                                    checked aria-label="No rating." />
                                <input type="radio" id="first-rate1" name="review[rating]" value="1" />
                                <label for="first-rate1" title="Terrible">1 star</label>
                                <input type="radio" id="first-rate2" name="review[rating]" value="2" />
                                <label for="first-rate2" title="Not good">2 stars</label>
                                <input type="radio" id="first-rate3" name="review[rating]" value="3" />
                                <label for="first-rate3" title="Average">3 stars</label>
                                <input type="radio" id="first-rate4" name="review[rating]" value="4" />
                                <label for="first-rate4" title="Very good">4 stars</label>
                                <input type="radio" id="first-rate5" name="review[rating]" value="5" />
                                <label for="first-rate5" title="Amazing">5 stars</label>
                            </fieldset>
                            <div class="mb-3 mt-3">
                                <label for="comment" class="form-label">Comment</label>
                                <textarea name="review[comment]" id="comment" class="form-control" rows="5" cols="30"
                                    required></textarea>
                                <div class="valid-feedback">Good</div>
                                <div class="invalid-feedback">Share your experience here</div>
                                <button type="submit" class="btn btn-outline-danger mb-3 mt-3">Submit</button>
                            </div>
                        </form>
                        <% } %>
        </div>
        <div class="col-6 offset-3 mb-3">
            <hr>
            <h3>Where you'll be</h3>
            <div id="map" class="mb-3 mt-3"></div>
        </div>
    </div>

    <script>
        const myAPIKey = "<%= process.env.MAP_API_KEY %>";
        const lat = "<%= listing.coordinates.latitude %>";
        const lon = "<%= listing.coordinates.longitude %>";

        const map = L.map("map").setView([lat, lon], 12); // Delhi, India

        L.tileLayer(`https://maps.geoapify.com/v1/tile/osm-bright/{z}/{x}/{y}.png?apiKey=${myAPIKey}`, {
            attribution:
                '© <a href="https://www.geoapify.com/">Geoapify</a> contributors',
        }).addTo(map);

        //  marker
        L.marker([lat, lon]).addTo(map).bindPopup(`<%= listing.title %>,<%= listing.location %>,<%= listing.country %>`);
    </script>